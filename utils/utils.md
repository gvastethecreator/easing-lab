# Utils

This directory contains utility functions that are shared across the application.

## Utility List

- **`gsapUtils.ts`**: Contains functions related to GSAP, specifically for generating SVG path data from GSAP easing strings to visualize the curves.
